# Security

Please report security issues privately to the repository owner.

`gh-suggest` shells out to `git` and `gh`, reads local diffs, and posts review comments only after preview/confirmation unless `--yes` is passed.
